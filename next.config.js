/** @type {import('next').NextConfig} */
const nextConfig = {
  // add the lines below ↓
  experimental: {
    serverActions: true,
  },
};
// module.exports = nextConfig;
module.exports = nextConfig;
